# DelphiAI Python dependencies
# Pinned to the versions validated in local development (Python 3.12).
# Regenerate intentionally — a raw `pip freeze` on Windows leaks win32-only
# packages that break the Linux container build.

# --- Web API ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
starlette==1.3.0
pydantic==2.13.4

# --- Database ---
psycopg2-binary==2.9.12
python-dotenv==1.2.2
yoyo-migrations==9.0.0

# --- ML / prediction pipeline ---
xgboost==3.2.0
scikit-learn==1.9.0
numpy==2.4.6
pandas==3.0.3
joblib==1.5.3

# --- Live predict-time injury scrape (lightweight; no browser) ---
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1

# --- Bulk scraping pipeline (GitHub Actions cron, weekly_update) ---
Scrapy==2.16.0
scrapy-playwright==0.0.46
